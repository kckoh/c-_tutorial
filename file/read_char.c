#include <stdio.h>
# define LINE_LENGTH (8)
int main(void){

    //get a character and return the character
    // int c = getchar();
    // while(c != EOF){
    //     putchar(c);
    //     c = getchar();
    // }

    // reads a line and return the line
    // char line[LINE_LENGTH];

    // while(fgets(line,LINE_LENGTH, stdin ) != NULL ){
    //     //puts writes strings and return non-negative
    //     printf("line: %s", line);
    // }

    return 0;
}